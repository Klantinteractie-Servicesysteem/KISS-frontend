version: "3.4"

services:
  npmtest:
    build:
      context: .
      dockerfile: Kiss.Bff/Dockerfile
      target: npmtest
    volumes:
      - ./testresults:/results
  dotnettest:
    build:
      context: .
      dockerfile: Kiss.Bff/Dockerfile
      target: dotnettest
    volumes:
      - ./testresults:/results
